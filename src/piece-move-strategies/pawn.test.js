const board = require('../classes/board');
const pawn = require('./pawn');

test('when white pawn', () => {
    test('when move 1 square forward', () => {
        test('should find pawn', () => {
            expect(1).toBe(2);
        });
    });
});
